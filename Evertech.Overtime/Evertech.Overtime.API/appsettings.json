{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DbOvertime": "${SECRET}"
  },
  "CryptographyKey": "${SECRET}",
  "EmailUser": "${SECRET}",
  "EmailHost": "${SECRET}",
  "EmailEnableSSL": "${SECRET}",
  "EmailUseDefaultCredentials": "${SECRET}",
  "EmailPassword": "${SECRET}",
  "EmailPort": "${SECRET}"
}